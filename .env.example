# Supabase — shared with the COCM Bookstore app (same project, same auth.users).
# One account logs into both apps.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
